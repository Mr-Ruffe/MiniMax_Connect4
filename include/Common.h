#ifndef COMMON_H
#define COMMON_H

namespace common {
    
enum class Turn {
    firstPlayer, secondPlayer
};

enum class SetupParameter {
    firstPlayer, secondPlayer, bothPlayer, noPlayer
};

};

#endif